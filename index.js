// 1. Multiply num1 and num2 and set the result to multiply
let num1 = 7; // You might need to adjust these values to get a product of 62
let num2 = 9;
let multiply = num1 * num2;
console.log("multiply:", multiply); // Check the value of multiply

// 2. Generate a random integer greater than 0 and set it to random
let random = Math.floor(Math.random() * 10) + 1;
console.log("random:", random); // Check the value of random

// 3. Calculate the remainder of dividing num3 by num4 and set it to mod
let num3 = 12;
let num4 = 8;
let mod = num3 % num4;
console.log("mod:", mod); // Check the value of mod

// 4. Find the highest number in a set (assuming set is {num1, num2, random}) and set it to max
max = Math.max(num1, num2, random);
console.log("max:", max); // Check the value of max